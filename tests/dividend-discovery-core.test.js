const assert=require('node:assert/strict');const test=require('node:test');const{normalizeDividendDiscovery}=require('../dividend-discovery-core');
test('joins only exact asset UID and positive verified yield',()=>{const r=normalizeDividendDiscovery([{assetUid:'a',uid:'ia',ticker:'AAA',name:'Alpha'},{assetUid:'b',uid:'ib',ticker:'BBB',name:'Beta'}],[{assetUid:'a',dividendYield:8.4,marketCap:1000},{assetUid:'b',dividendYield:0},{assetUid:'x',dividendYield:99}]);assert.equal(r.rows.length,1);assert.equal(r.rows[0].ticker,'AAA');assert.equal(r.rows[0].dividendYield,8.4);assert.deepEqual(r.coverage,{shares:2,fundamentals:3,dividendRows:1,duplicateFundamentals:0});});
test('is deterministic and rejects duplicate fundamental identities',()=>{const r=normalizeDividendDiscovery([{assetUid:'a',uid:'ia',ticker:'AAA'},{assetUid:'b',uid:'ib',ticker:'BBB'}],[{assetUid:'a',dividendYield:4},{assetUid:'a',dividendYield:40},{assetUid:'b',dividendYield:9}]);assert.deepEqual(r.rows.map(x=>x.ticker),['BBB','AAA']);assert.equal(r.coverage.duplicateFundamentals,1);});
