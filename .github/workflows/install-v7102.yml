name: Install v7.10.2 benchmark guard
'on':
  push:
    paths:
      - '.github/workflows/install-v7102.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install overview benchmark guard
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('public/index.html');s=p.read_text();tag='<script src="/v7102.js?v=7.10.2"></script>'
          if tag not in s:
            pos=s.lower().rfind('</body>')
            if pos<0: raise SystemExit('body close not found')
            p.write_text(s[:pos]+tag+s[pos:])
          PY
          node --check public/v7102.js
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add public/index.html public/v7102.js
          git commit -m 'v7.10.2 stabilize VS MOEX display' || exit 0
          git push
