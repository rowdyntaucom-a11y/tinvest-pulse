# 2026-09-24 — Cosmos Blue Singularity / Sentinel Order shell

Starting main SHA: `32843c08d4a2d2eae813cec32ff90262706382b7`.

## Why this pass

The active Cosmos implementation still described itself as an orbital observatory, which conflicts with the user-approved Mix C direction. The latest continuity review also found two blockers: the canonical roadmap still told contributors to finish Samurai first, and the temporary fal master-art upload route reset the socket for oversized uploads instead of returning a useful HTTP error.

## What changed

- Cosmos is now the active serial shell in `QVANIX_MASTER_DIRECTION.md`; Samurai remains preserved rather than reopened for broad iteration.
- Replaced the old observatory-oriented `cosmosSignatureSystem.css` with a Blue Singularity / Sentinel Order system.
- New Cosmos language uses saturated cobalt/sapphire/electric-cyan/icy-white with restrained violet, asymmetrical sentinel slabs, clipped architecture, docking-beacon navigation, workspace-specific sentinel chrome and open visual space for the approved master.
- Motion is limited to transform/opacity-based star parallax, singularity breathing, sentinel depth and docking feedback, with a complete reduced-motion fallback.
- Narrow-phone rules remove expensive topbar/card blur and preserve readable first-screen composition.
- The Home art hook remains deliberately art-ready but does not pretend that a CSS/generated replacement is the approved Mix C master.
- The exact sharp Mix C bytes are still not stored in the repository. Final artwork mounting must use that exact original asset, never the rejected blurry fal.ai derivative.
- Fixed `POST /api/art/upload` over-limit handling: uploads above 10 MiB now return explicit HTTP 413 JSON instead of destroying the socket.
- Financial calculations, broker access, trust-state semantics, navigation ownership and DNA logic were not changed.

## Review focus

1. Mobile 360–430 px: first viewport still answers; Cosmos atmosphere is visible without covering the hero value.
2. Theme distinction: Cosmos must no longer read like Neon or the rejected observatory concept.
3. Performance: no animated blur/filter loops were added; motion is transform/opacity and reduced-motion safe.
4. Art continuity: do not merge any later patch that points Cosmos at `cosmos-mixc-fal.html` or its blurry image as the production master.
5. Upload boundary: >10 MiB art upload returns 413 without connection reset.

## Remaining real-device checkpoint

After merge/deploy, validate on the user's Samsung recording:
- hero value remains readable over the right-side singularity reserve;
- theme switch remains smooth;
- scrolling does not stutter;
- bottom navigation remains reachable;
- Cosmos reads as a distinct world across Home, Assets, Analysis, Income and Goal;
- exact Mix C integration remains pending until the original sharp asset is durably persisted.
