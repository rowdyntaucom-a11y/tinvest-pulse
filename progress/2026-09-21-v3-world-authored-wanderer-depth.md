# QVANIX v3 — authored wanderer depth

Date: 2026-09-21

## Real-device basis
The Samsung capture after the settlement composition pass confirms that workshop/mine spacing is now readable and duplicate geometry is gone. The hero is the clearest remaining prototype element and visually loses against the authored structures.

## Changes
- Add a dedicated `actors.hero-wanderer` slot to the canonical world asset contract.
- Package a transparent reviewed-local wanderer asset in both shared public roots.
- Promote the wanderer through the same readiness/load/mount policy as reviewed structures.
- Preserve the existing procedural hero body as fail-closed fallback and keep its shadow/movement container, so idle motion and positioning remain owned by the canonical renderer.
- Expose hero mount diagnostics and extend authored-asset safety tests to the new asset.

## Boundary
This is visual depth only. No XP/progression derivation and no financial values enter the DNA renderer.
