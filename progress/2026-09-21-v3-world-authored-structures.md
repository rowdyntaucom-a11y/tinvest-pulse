# QVANIX v3 — first authored structure promotion

Date: 2026-09-21

## Real-device basis
The restored Samsung capture confirms the runtime/camera foundation is working again. The visible quality ceiling is now the procedural starter geometry, especially the workshop and mine.

## Changes
- Add locally packaged, transparent authored SVG assets for the starter workshop and mine entrance.
- Register both through the existing reviewed-local manifest with explicit provenance.
- Package the shared manifest assets for both v2 and v3 public roots so the shared contract never points at a missing local file.
- Mount each asset independently through the canonical readiness/load/mount policy. Failure of one slot no longer prevents the other reviewed slots from being attempted.
- Keep the procedural starter scene underneath as fail-closed fallback/context.
- Expose workshop/mine mount diagnostics and add SVG safety + mount regressions to the full v3 suite.

## Boundary
No financial/progression methodology changes. Level remains 1 / XP 0. This is the first infrastructure-backed authored-asset promotion, not a claim that final art direction is complete.
