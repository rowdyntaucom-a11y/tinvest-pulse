import{useState}from"react";
const STEPS=[
 {kicker:"01 · Пульт",title:"Сначала — ответ",text:"Капитал, TWR, денежный результат и пассивный доход собраны в первом экране. Демо использует только синтетический сценарий."},
 {kicker:"02 · Аналитика",title:"Потом — объяснение",text:"Откройте «Аналитику» в нижней навигации: риск, структура и сравнение с IMOEX раскрываются глубже без превращения интерфейса в торговый терминал."},
 {kicker:"03 · Доход",title:"Факт отдельно от ожиданий",text:"В «Доходе» полученные выплаты и качество покрытия разделены. Неподтверждённые будущие выплаты не становятся прогнозом."},
 {kicker:"04 · Цель",title:"Сценарий, не обещание",text:"В «Цели» введённые предпосылки отделены от факта: сценарная траектория не выдаётся за прогноз."},
] as const;
export function V3DemoGuide(){const[index,setIndex]=useState(0),[open,setOpen]=useState(true),step=STEPS[index];if(!open)return <button className="v3-demo-guide-reopen" type="button" onClick={()=>setOpen(true)}>Как читать демо</button>;return <aside className="v3-demo-guide" aria-label="Короткий тур по QVANIX"><div className="v3-demo-guide-head"><span>{step.kicker}</span><button type="button" aria-label="Закрыть тур" onClick={()=>setOpen(false)}>×</button></div><strong>{step.title}</strong><p>{step.text}</p><div className="v3-demo-guide-progress" aria-label={`Шаг ${index+1} из ${STEPS.length}`}>{STEPS.map((_,i)=><i key={i} className={i===index?"is-active":""}/>)}</div><div className="v3-demo-guide-actions">{index>0&&<button type="button" onClick={()=>setIndex(index-1)}>Назад</button>}{index<STEPS.length-1?<button type="button" onClick={()=>setIndex(index+1)}>Дальше</button>:<a href="/v3/">Открыть свой портфель</a>}</div><small>Реальные данные появятся только в live-режиме после настроенного read-only источника.</small></aside>}
