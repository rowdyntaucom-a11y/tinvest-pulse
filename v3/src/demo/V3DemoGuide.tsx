import{useState}from"react";
const STEPS=[
 {eyebrow:"01 · ПУЛЬТ",title:"Сначала — ответ",body:"Первый экран показывает капитал, результат, доход и ключевые показатели. Это синтетический портфель: цифры нужны только для знакомства с продуктом."},
 {eyebrow:"02 · ПОРТФЕЛЬ",title:"От суммы к структуре",body:"Откройте «Портфель», чтобы увидеть веса, классы активов и перейти в единую карточку инструмента. QVANIX не превращает структуру в команды купить или продать."},
 {eyebrow:"03 · АНАЛИТИКА",title:"Риск с объяснением",body:"В «Аналитике» собраны сравнение с IMOEX, концентрация и риск. Сложные показатели раскрываются через контекст, а неподтверждённые данные скрываются."},
 {eyebrow:"04 · ДОХОД И ЦЕЛЬ",title:"Длинный горизонт",body:"«Доход» объясняет денежный поток, а «Цель» — сценарии движения к ориентиру. Сценарий не является прогнозом или обещанием результата."}
]as const;
export function V3DemoGuide(){const[open,setOpen]=useState(true),[step,setStep]=useState(0);if(!open)return <button className="v3-demo-guide-reopen" type="button" onClick={()=>{setStep(0);setOpen(true)}}>ТУР</button>;const item=STEPS[step],last=step===STEPS.length-1;return <aside className="v3-demo-guide" aria-label="Знакомство с QVANIX"><div className="v3-demo-guide-head"><span>{item.eyebrow}</span><button type="button" onClick={()=>setOpen(false)} aria-label="Закрыть подсказки">×</button></div><strong>{item.title}</strong><p>{item.body}</p><div className="v3-demo-guide-progress" aria-label={`Шаг ${step+1} из ${STEPS.length}`}>{STEPS.map((_,i)=><i key={i} className={i<=step?"is-active":""}/>)}</div><div className="v3-demo-guide-actions">{step>0&&<button type="button" onClick={()=>setStep(v=>v-1)}>Назад</button>}{last?<a href="/v3/">Открыть мой портфель</a>:<button className="is-primary" type="button" onClick={()=>setStep(v=>v+1)}>Дальше</button>}</div><small>Демо не подключается к брокеру и не экспортируется в Пульс.</small></aside>}
