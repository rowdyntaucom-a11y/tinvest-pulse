import{useState}from"react";import type{HistoryPoint}from"../../../v2/src/lib/portfolioApi";import{V3HistoryWindowControl}from"../history/V3HistoryWindowControl";import{filterHistoryWindow,summarizeHistoryValue,type V3HistoryWindow}from"../history/historyLens";const money=new Intl.NumberFormat("ru-RU",{notation:"compact",maximumFractionDigits:1}),deltaMoney=new Intl.NumberFormat("ru-RU",{notation:"compact",maximumFractionDigits:1,signDisplay:"exceptZero"}),pct=new Intl.NumberFormat("ru-RU",{maximumFractionDigits:1,signDisplay:"exceptZero"}),date=(v:string)=>{const d=new Date(v);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat("ru-RU",{day:"2-digit",month:"short"}).format(d)};export function V3HistorySparkline({points,detailed=false}:{points:HistoryPoint[];detailed?:boolean}){const[window,setWindow]=useState<V3HistoryWindow>("all"),activeWindow=detailed?window:"all",windowPoints=filterHistoryWindow(points,activeWindow),summary=summarizeHistoryValue(windowPoints),rows=windowPoints.filter(x=>x.value!=null);if(!summary||rows.length<2)return <div className="v3-chart-empty">История появится после подтверждения данных</div>;const numeric=windowPoints.flatMap(x=>[x.value,x.invested]).filter((x):x is number=>x!=null),min=Math.min(...numeric),max=Math.max(...numeric),span=max-min||1,x=(i:number)=>windowPoints.length<2?0:(i/(windowPoints.length-1))*100,y=(v:number)=>44-((v-min)/span)*36,values=windowPoints.map((p,i)=>p.value==null?null:{x:x(i),y:y(p.value)}).filter((p):p is{x:number;y:number}=>p!=null),invested=windowPoints.map((p,i)=>p.invested==null?null:{x:x(i),y:y(p.invested)}).filter((p):p is{x:number;y:number}=>p!=null),line=values.map(p=>`${p.x},${p.y}`).join(" "),investedLine=invested.map(p=>`${p.x},${p.y}`).join(" "),area=`${values[0].x},46 ${line} ${values.at(-1)!.x},46`;return <figure className="v3-history-figure">{detailed&&<V3HistoryWindowControl value={window} onChange={setWindow}/>}<svg className="v3-chart" viewBox="0 0 100 48" preserveAspectRatio="none" role="img" aria-label="История стоимости портфеля"><defs><linearGradient id="v3HistoryFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="currentColor" stopOpacity=".22"/><stop offset="1" stopColor="currentColor" stopOpacity="0"/></linearGradient></defs><line className="v3-chart-grid" x1="0" y1="24" x2="100" y2="24"/><polygon className="v3-chart-area" points={area}/>{detailed&&invested.length>1&&<polyline className="v3-chart-invested" points={investedLine}/>}<polyline points={line}/><circle className="v3-chart-end" cx={values.at(-1)!.x} cy={values.at(-1)!.y} r="1.5"/></svg>{detailed&&invested.length>1&&<div className="v3-history-legend" aria-label="Легенда графика"><span><i/>Стоимость</span><span><i className="is-invested"/>Внесено</span></div>}<figcaption><span>{date(rows[0].date)} · {money.format(summary.startValue)} ₽</span><strong className={summary.deltaPct==null?"is-neutral":summary.deltaPct>0?"is-positive":summary.deltaPct<0?"is-negative":"is-neutral"}>{summary.deltaPct==null?"—":pct.format(summary.deltaPct)+"%"}</strong><span>{date(rows.at(-1)!.date)} · {money.format(summary.endValue)} ₽</span></figcaption>{detailed&&<section className="v3-history-depth" aria-label="Детали выбранного периода"><div><span>Стоимость Δ</span><strong>{deltaMoney.format(summary.deltaValue)} ₽</strong><small>изменение стоимости, не доходность</small></div><div><span>Диапазон</span><strong>{money.format(summary.minValue)}–{money.format(summary.maxValue)} ₽</strong><small>{summary.points} точек</small></div><div><span>Внесено Δ</span><strong>{summary.investedDelta==null?"—":deltaMoney.format(summary.investedDelta)+" ₽"}</strong><small>по подтверждённой истории</small></div></section>}</figure>}