import type{HistoryPoint}from"../../../v2/src/lib/portfolioApi";const money=new Intl.NumberFormat("ru-RU",{notation:"compact",maximumFractionDigits:1}),pct=new Intl.NumberFormat("ru-RU",{maximumFractionDigits:1,signDisplay:"exceptZero"}),date=(v:string)=>{const d=new Date(v);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat("ru-RU",{day:"2-digit",month:"short"}).format(d)};export function V3HistorySparkline({points}:{points:HistoryPoint[]}){const rows=points.filter(x=>x.value!=null);if(rows.length<2)return <div className="v3-chart-empty">История появится после подтверждения данных</div>;const values=rows.map(x=>x.value as number),min=Math.min(...values),max=Math.max(...values),span=max-min||1,coords=values.map((v,i)=>`${(i/(values.length-1))*100},${44-((v-min)/span)*36}`),line=coords.join(" "),area=`0,46 ${line} 100,46`,first=values[0],last=values.at(-1)!,change=first!==0?(last/first-1)*100:null;return <figure className="v3-history-figure"><svg className="v3-chart" viewBox="0 0 100 48" preserveAspectRatio="none" role="img" aria-label="История стоимости портфеля"><defs><linearGradient id="v3HistoryFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="currentColor" stopOpacity=".22"/><stop offset="1" stopColor="currentColor" stopOpacity="0"/></linearGradient></defs><line className="v3-chart-grid" x1="0" y1="24" x2="100" y2="24"/><polygon className="v3-chart-area" points={area}/><polyline points={line}/><circle className="v3-chart-end" cx="100" cy={coords.at(-1)!.split(",")[1]} r="1.5"/></svg><figcaption><span>{date(rows[0].date)} · {money.format(first)} ₽</span><strong className={change!=null&&change<0?"is-negative":""}>{change==null?"—":pct.format(change)+"%"}</strong><span>{date(rows.at(-1)!.date)} · {money.format(last)} ₽</span></figcaption></figure>}
