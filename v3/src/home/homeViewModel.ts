import type{PortfolioSnapshot,HistoryPoint}from"../../../v2/src/lib/portfolioApi";
export type V3HomePosition={ticker:string;name:string;weight:number;currentValue:number;expectedYield:number};export type V3HomeViewModel={accountName:string;value:number|null;profit:number|null;profitPct:number|null;passiveIncome:number|null;positions:number|null;cagr:number|null;xirr:number|null;updatedAt:string|null;history:HistoryPoint[];leaders:V3HomePosition[];isTrusted:boolean};
export function buildV3HomeViewModel(snapshot:PortfolioSnapshot,isTrusted:boolean):V3HomeViewModel{const visible=isTrusted&&snapshot.source!=="fallback";const leaders=visible?[...snapshot.positionItems].sort((a,b)=>b.currentValue-a.currentValue).slice(0,3).map(x=>({ticker:x.ticker,name:x.name,weight:x.weight,currentValue:x.currentValue,expectedYield:x.expectedYield})):[];return{accountName:snapshot.accountName||"Портфель",value:visible?snapshot.value:null,profit:visible?snapshot.profit:null,profitPct:visible?snapshot.profitPct:null,passiveIncome:visible?snapshot.passiveIncome:null,positions:visible?snapshot.positions:null,cagr:visible?snapshot.cagr:null,xirr:visible?snapshot.xirr:null,updatedAt:visible?snapshot.updatedAt:null,history:visible?snapshot.history:[],leaders,isTrusted:visible}}
