import{useEffect,useState}from"react";import{V3_NAV,type V3DetailMode,type V3Shell,type V3Workspace}from"./model";import{V3Home}from"../home/V3Home";import type{V3HomeViewModel}from"../home/homeViewModel";import{V3Assets}from"../assets/V3Assets";import type{HistoryPoint,PositionSnapshot}from"../../../v2/src/lib/portfolioApi";import{V3Analysis}from"../analysis/V3Analysis";import{V3Income,type V3IncomeModel}from"../income/V3Income";import{V3Goal}from"../goal/V3Goal";const PREF="qvanix.v3.preferences";type Prefs={shell:V3Shell;mode:V3DetailMode;workspace:V3Workspace};const fallback:Prefs={shell:"core",mode:"simple",workspace:"home"};function loadPrefs():Prefs{try{const x=JSON.parse(localStorage.getItem(PREF)||"{}");return{shell:["core","horizon","carbon"].includes(x.shell)?x.shell:fallback.shell,mode:["simple","detailed"].includes(x.mode)?x.mode:fallback.mode,workspace:V3_NAV.some(n=>n.id===x.workspace)?x.workspace:fallback.workspace}}catch{return fallback}}export function V3App({home,positions,history,income}:{home:V3HomeViewModel;positions:PositionSnapshot[];history:HistoryPoint[];income:V3IncomeModel}){const initial=loadPrefs(),[workspace,setWorkspace]=useState<V3Workspace>(initial.workspace),[shell,setShell]=useState<V3Shell>(initial.shell),[mode,setMode]=useState<V3DetailMode>(initial.mode);useEffect(()=>{localStorage.setItem(PREF,JSON.stringify({workspace,shell,mode}))},[workspace,shell,mode]);return <div className="v3-app" data-shell={shell}><div className="v3-controls"><button aria-label="Режим интерфейса" onClick={()=>setMode(mode==="simple"?"detailed":"simple")}>{mode==="simple"?"Просто":"Подробно"}</button><select aria-label="Оболочка" value={shell} onChange={e=>setShell(e.target.value as V3Shell)}><option value="core">CORE</option><option value="horizon">HORIZON</option><option value="carbon">CARBON</option></select></div>{workspace==="home"?<V3Home model={home} shell={shell} mode={mode}/>:workspace==="assets"?<V3Assets items={positions} trusted={home.isTrusted} shell={shell} mode={mode}/>:workspace==="analysis"?<V3Analysis items={positions} history={history} trusted={home.isTrusted} shell={shell} mode={mode}/>:workspace==="income"?<V3Income model={income} shell={shell} mode={mode}/>:workspace==="goal"?<V3Goal value={home.value} trusted={home.isTrusted} shell={shell} mode={mode}/>:<V3Home model={home} shell={shell} mode={mode}/> }<nav className="v3-nav" aria-label="Основные разделы">{V3_NAV.map(x=><button key={x.id} aria-current={workspace===x.id?"page":undefined} className={workspace===x.id?"is-active":""} onClick={()=>setWorkspace(x.id)}><span>{x.label}</span></button>)}</nav></div>}
