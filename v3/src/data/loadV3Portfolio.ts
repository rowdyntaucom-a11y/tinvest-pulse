import{loadPortfolio,type PortfolioSnapshot}from"../../../v2/src/lib/portfolioApi";import{evaluateDataTrust,type DataTrustSnapshot}from"../../../v2/src/lib/dataTrust";
export type V3PortfolioLoad={snapshot:PortfolioSnapshot;trust:DataTrustSnapshot};
export async function loadV3Portfolio():Promise<V3PortfolioLoad>{const snapshot=await loadPortfolio();const hasData=snapshot.source!=="fallback"&&snapshot.updatedAt!=null;const sourceState=snapshot.source==="fallback"?"FALLBACK":"LIVE";const trust=evaluateDataTrust({sourceId:snapshot.source==="dashboard"?"DASHBOARD":snapshot.source==="portfolio"?"PORTFOLIO":null,sourceType:snapshot.source==="fallback"?"CACHE":"BROKER",sourceState,fetchedAt:snapshot.updatedAt,sourceTimestamp:snapshot.updatedAt,coverage:hasData?"COMPLETE":"UNKNOWN",hasData,nowMs:Date.now(),staleAfterMs:15*60_000});return{snapshot,trust}}
