import type{HistoryPoint,PositionSnapshot}from"../../../v2/src/lib/portfolioApi";import type{V3HomeViewModel}from"../home/homeViewModel";import type{V3LoadState}from"./loadState";import"../styles/dataQuality.css";
const stamp=(value:string|null)=>{if(!value)return"не указано";const d=new Date(value);return Number.isNaN(d.getTime())?"не указано":new Intl.DateTimeFormat("ru-RU",{day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit"}).format(d)};
const age=(value:string|null)=>{if(!value)return null;const ms=Date.now()-new Date(value).getTime();if(!Number.isFinite(ms)||ms<0)return null;const min=Math.floor(ms/60000);return min<1?"меньше минуты":min<60?`${min} мин`:min<1440?`${Math.floor(min/60)} ч ${min%60} мин`:`${Math.floor(min/1440)} дн`};
const stateLabel:Record<V3LoadState,string>={loading:"синхронизация",live:"подтверждено",partial:"частично",stale:"устарело",fallback:"резерв",error:"ошибка",unavailable:"нет данных"};
export function V3DataQualityPanel({home,positions,history,loadState}:{home:V3HomeViewModel;positions:PositionSnapshot[];history:HistoryPoint[];loadState:V3LoadState}){const trusted=home.isTrusted&&loadState==="live",confirmedHistory=trusted?history.filter(point=>point.value!=null).length:0,snapshotAge=trusted?age(home.updatedAt):null;return <section className="v3-data-quality" aria-label="Качество и свежесть данных"><header><span>КОНТУР ДОВЕРИЯ</span><strong>{trusted?"Расчёты открыты":"Расчёты закрыты"}</strong></header><div className="v3-data-quality-grid"><article><span>Состояние</span><strong>{stateLabel[loadState]}</strong><small>{trusted?"полный LIVE-контур":"fail-closed"}</small></article><article><span>Снимок</span><strong>{trusted?stamp(home.updatedAt):"—"}</strong><small>{snapshotAge?`возраст · ${snapshotAge}`:"свежесть не подтверждена"}</small></article><article><span>Позиции</span><strong>{trusted?positions.length:"—"}</strong><small>{trusted?"из текущего снимка":"не используются"}</small></article><article><span>История</span><strong>{trusted?confirmedHistory:"—"}</strong><small>{trusted?`${history.length-confirmedHistory} пропусков сохранено`:"не используется"}</small></article></div><p>{trusted?"QVANIX использует только подтверждённый LIVE-снимок. Пропуски истории сохраняются как пропуски и не дорисовываются.":"Неполные, устаревшие, резервные и недоступные данные не становятся нулями и не допускаются к финансовым расчётам."}</p></section>}
