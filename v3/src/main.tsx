import React,{useEffect,useState}from"react";import ReactDOM from"react-dom/client";import{V3App}from"./app/V3App";import{buildV3HomeViewModel}from"./home/homeViewModel";import{loadV3Portfolio}from"./data/loadV3Portfolio";import{isV3VerifiedLive,toV3LoadState,type V3LoadState}from"./data/loadState";import"./styles/v3.css";import"./styles/goal.css";import"./styles/dataState.css";import"./styles/shellWorlds.css";import"./styles/assetInspector.css";import"./styles/assetWorkspace.css";import"./styles/incomeStory.css";import"./styles/analysisDepth.css";import type{PortfolioSnapshot}from"../../v2/src/lib/portfolioApi";
const EMPTY={accountName:"Кряхтящий фонд",value:0,profit:0,profitPct:0,passiveIncome:0,averageMonthlyPassiveIncome:0,averageAnnualPassiveIncome:0,positions:0,positionItems:[],xirr:null,cagr:null,riskFreeRate:null,riskFreeRateDate:null,nextRateMeeting:null,startDate:null,updatedAt:null,history:[],source:"fallback"} satisfies PortfolioSnapshot;
function Root(){const[snapshot,setSnapshot]=useState<PortfolioSnapshot>(EMPTY),[trusted,setTrusted]=useState(false),[loadState,setLoadState]=useState<V3LoadState>("loading");useEffect(()=>{let active=true;loadV3Portfolio().then(({snapshot,trust})=>{if(active){setSnapshot(snapshot);setTrusted(isV3VerifiedLive(trust));setLoadState(toV3LoadState(trust))}}).catch(()=>{if(active){setTrusted(false);setLoadState("error")}});return()=>{active=false}},[]);return <V3App loadState={loadState} home={buildV3HomeViewModel(snapshot,trusted)} positions={trusted?snapshot.positionItems:[]} history={trusted?snapshot.history:[]} market={{riskFreeRate:trusted?snapshot.riskFreeRate:null,riskFreeRateDate:trusted?snapshot.riskFreeRateDate:null,nextRateMeeting:trusted?snapshot.nextRateMeeting:null}} income={{total:trusted?snapshot.passiveIncome:null,monthly:trusted?snapshot.averageMonthlyPassiveIncome:null,annual:trusted?snapshot.averageAnnualPassiveIncome:null,portfolioValue:trusted?snapshot.value:null,trusted}}/>};ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><Root/></React.StrictMode>);
