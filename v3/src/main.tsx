import React,{useEffect,useState}from"react";import ReactDOM from"react-dom/client";import{V3App,type V3LoadState}from"./app/V3App";import{buildV3HomeViewModel}from"./home/homeViewModel";import{loadV3Portfolio}from"./data/loadV3Portfolio";import"./styles/v3.css";import"./styles/goal.css";import type{PortfolioSnapshot}from"../../v2/src/lib/portfolioApi";
const EMPTY={accountName:"Кряхтящий фонд",value:0,profit:0,profitPct:0,passiveIncome:0,averageMonthlyPassiveIncome:0,averageAnnualPassiveIncome:0,positions:0,positionItems:[],xirr:null,cagr:null,riskFreeRate:null,riskFreeRateDate:null,nextRateMeeting:null,startDate:null,updatedAt:null,history:[],source:"fallback"} satisfies PortfolioSnapshot;
function Root(){const[snapshot,setSnapshot]=useState<PortfolioSnapshot>(EMPTY),[trusted,setTrusted]=useState(false),[loadState,setLoadState]=useState<V3LoadState>("loading");useEffect(()=>{let active=true;loadV3Portfolio().then(({snapshot,trust})=>{if(active){setSnapshot(snapshot);setTrusted(trust.safeToDisplay);setLoadState(trust.safeToDisplay?"live":"untrusted")}}).catch(()=>{if(active){setTrusted(false);setLoadState("error")}});return()=>{active=false}},[]);return <V3App loadState={loadState} home={buildV3HomeViewModel(snapshot,trusted)} positions={trusted?snapshot.positionItems:[]} history={trusted?snapshot.history:[]} income={{total:trusted?snapshot.passiveIncome:null,monthly:trusted?snapshot.averageMonthlyPassiveIncome:null,annual:trusted?snapshot.averageAnnualPassiveIncome:null,trusted}}/>};ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><Root/></React.StrictMode>);
