import type{HistoryPoint,PositionSnapshot}from"../../../v2/src/lib/portfolioApi";import type{V3DetailMode,V3Shell}from"../app/model";const n=new Intl.NumberFormat("ru-RU",{maximumFractionDigits:1});function concentration(items:PositionSnapshot[]){return items.reduce((s,x)=>s+x.weight*x.weight,0)}function diversification(hhi:number){return hhi<=1250?"Высокая":hhi<=2500?"Средняя":"Концентрация"}function drawdown(history:HistoryPoint[]){let peak=-Infinity,worst=0;for(const p of history){if(p.value==null)continue;peak=Math.max(peak,p.value);if(peak>0)worst=Math.min(worst,(p.value/peak-1)*100)}return Number.isFinite(worst)?worst:null}export function V3Analysis({items,history,trusted,shell,mode}:{items:PositionSnapshot[];history:HistoryPoint[];trusted:boolean;shell:V3Shell;mode:V3DetailMode}){const rows=trusted?items:[],hhi=concentration(rows),dd=trusted?drawdown(history):null,ranked=[...rows].sort((a,b)=>b.weight-a.weight),top=ranked.slice(0,3).reduce((s,x)=>s+x.weight,0),largest=ranked[0]??null,positive=rows.filter(x=>x.expectedYield>0).length;return <main className="v3-analysis" data-shell={shell}><header className="v3-page-head"><span>ПОРТФЕЛЬ · ДИАГНОСТИКА</span><h1>Анализ</h1><p>{trusted?"Структура и риск · без торговых рекомендаций":"Аналитика скрыта до подтверждения данных"}</p></header>{trusted&&<section className="v3-analysis-signal"><div><span>Структура</span><strong>{diversification(hhi)}</strong><small>HHI {n.format(hhi)} · топ-3 {n.format(top)}%</small></div><div><span>Крупнейшая</span><strong>{largest?.ticker??"—"}</strong><small>{largest?n.format(largest.weight)+"% портфеля":"—"}</small></div></section>}<section className="v3-analysis-grid"><article><span>Макс. просадка</span><strong>{dd==null?"—":n.format(dd)+"%"}</strong><small>{history.length+" точек истории"}</small></article><article><span>В плюсе</span><strong>{trusted?positive+"/"+rows.length:"—"}</strong><small>По текущему результату</small></article><article><span>Топ-3 позиций</span><strong>{trusted?n.format(top)+"%":"—"}</strong><small>Концентрация капитала</small></article><article><span>Позиций</span><strong>{trusted?rows.length:"—"}</strong><small>Подтверждённый состав</small></article></section>{mode==="detailed"&&<section className="v3-analysis-detail"><h2>Карта концентрации</h2>{ranked.slice(0,5).map(x=><div key={x.figi||x.ticker}><span>{x.ticker}</span><i><b style={{width:Math.min(100,x.weight)+"%"}}/></i><strong>{n.format(x.weight)}%</strong></div>)}</section>}<section className="v3-analysis-note">Показатели описывают текущую структуру и доступную историю. Это диагностика портфеля, а не рекомендация купить или продать актив.</section></main>}
