import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{nextRefreshDelay,shouldApplyRefresh,shouldRefreshOnFocus,V3_FOCUS_REFRESH_MIN_AGE_MS,V3_REFRESH_INTERVAL_MS}from"../src/data/refreshPolicy.ts";
assert.equal(V3_REFRESH_INTERVAL_MS,60_000);assert.equal(shouldApplyRefresh(4,4,true),true);assert.equal(shouldApplyRefresh(3,4,true),false);assert.equal(shouldApplyRefresh(4,4,false),false);assert.equal(shouldRefreshOnFocus(null,100_000),true);assert.equal(shouldRefreshOnFocus(100_000,100_000+V3_FOCUS_REFRESH_MIN_AGE_MS-1),false);assert.equal(shouldRefreshOnFocus(100_000,100_000+V3_FOCUS_REFRESH_MIN_AGE_MS),true);assert.equal(nextRefreshDelay(null,1),0);assert.equal(nextRefreshDelay(10_000,20_000),50_000);assert.equal(nextRefreshDelay(10_000,80_000),0);
const main=readFileSync(new URL("../src/main.tsx",import.meta.url),"utf8"),app=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8"),css=readFileSync(new URL("../src/styles/dataState.css",import.meta.url),"utf8");assert.match(main,/setInterval\(\(\)=>void refresh\(\),V3_REFRESH_INTERVAL_MS\)/);assert.match(main,/visibilitychange/);assert.match(main,/addEventListener\("focus"/);assert.match(main,/runningRef\.current/);assert.match(main,/shouldApplyRefresh/);assert.match(app,/Обновить сейчас/);assert.match(app,/Последняя попытка/);assert.match(app,/disabled=\{refreshing\|\|!onRefresh\}/);assert.match(css,/\.v3-state-recovery/);assert.match(css,/min-height:44px/);console.log("v3 live refresh and recovery contracts: ok");
