import assert from"node:assert/strict";import{readFileSync}from"node:fs";const home=readFileSync(new URL("../src/home/V3Home.tsx",import.meta.url),"utf8"),css=readFileSync(new URL("../src/styles/readability.css",import.meta.url),"utf8");assert.match(home,/aria-label="Накопленный результат позиций"/);assert.match(home,/Лучший P\/L/);assert.match(home,/Слабейший P\/L/);assert.match(home,/накопленный брокерский результат, не дневное изменение цены и не рейтинг актива/);assert.doesNotMatch(home,/>Сильнее</);assert.doesNotMatch(home,/>Слабее</);assert.match(css,/\.v3-position-result-note\{/);console.log("v3 home position-result semantics: ok");
