import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/goal/V3Goal.tsx",import.meta.url),"utf8"),a=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8");assert.match(s,/qvanix\.v3\.goal\.target/);assert.match(s,/value\/target\*100/);assert.match(s,/Math\.max\(0,target-value\)/);assert.match(s,/не предполагает будущую доходность/);assert.match(s,/не обещает дату достижения/);assert.match(a,/workspace===\"goal\"/);assert.doesNotMatch(s,/CAGR|xirr|expectedYield|Math\.random/);console.log("v3 Goal contracts: ok");
