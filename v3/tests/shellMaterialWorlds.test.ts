import assert from"node:assert/strict";import{readFileSync}from"node:fs";const c=readFileSync(new URL("../src/styles/shellWorlds.css",import.meta.url),"utf8"),m=readFileSync(new URL("../src/main.tsx",import.meta.url),"utf8");assert.match(m,/shellWorlds\.css/);for(const shell of["core","horizon","carbon"])assert.match(c,new RegExp(`data-shell=["\\']${shell}`));assert.match(c,/v3-atmosphere/);assert.match(c,/prefers-reduced-motion:reduce/);assert.match(c,/max-width:359px/);assert.match(c,/core[^}]*v3-atmosphere/s);assert.match(c,/horizon[^}]*v3-atmosphere/s);assert.match(c,/carbon[^}]*v3-atmosphere/s);assert.match(c,/border-radius:22px 12px 22px 12px/);assert.match(c,/border-radius:14px/);assert.match(c,/border-radius:10px 22px 10px 22px/);console.log("v3 shell material worlds: ok");
