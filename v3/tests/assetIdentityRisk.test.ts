import assert from"node:assert/strict";import{readFileSync}from"node:fs";const w=readFileSync(new URL("../src/assets/V3AssetWorkspace.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/assetWorkspace.css",import.meta.url),"utf8");assert.match(w,/loadInstrumentBadges/);assert.match(w,/findInstrumentBadge/);assert.match(w,/instrumentLogoUrl/);assert.match(w,/instrumentFallbackLabel/);assert.match(w,/v3-asset-brand-mark/);assert.match(w,/{value:"risk",label:"Риск"/);assert.match(w,/summarizeAssetRisk/);assert.match(w,/Макс\. просадка/);assert.match(w,/концентрация капитала, не риск-вклад/);assert.match(w,/Структура облигации/);assert.match(w,/floatingCoupon/);assert.match(w,/amortizing/);assert.match(w,/YTM и duration не показываются/);assert.match(c,/v3-asset-risk-grid/);assert.match(c,/v3-bond-context/);assert.match(w,/V3SectionSelector label="Раздел актива"/);assert.doesNotMatch(c,/v3-asset-workspace-tabs/);assert.doesNotMatch(w,/annualizedVolatility|Math\.random|купить|продать/i);console.log("v3 asset identity and risk contracts: ok");
