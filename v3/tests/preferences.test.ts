import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8");assert.match(s,/qvanix\.v3\.preferences/);assert.match(s,/localStorage\.getItem/);assert.match(s,/localStorage\.setItem/);assert.match(s,/\["core","horizon","carbon","samurai"\]/);assert.match(s,/\["simple","detailed"\]/);assert.match(s,/V3_NAV\.some/);assert.match(s,/aria-current/);assert.match(s,/aria-label=\"Режим интерфейса\"/);console.log("v3 persistent preference contracts: ok");
