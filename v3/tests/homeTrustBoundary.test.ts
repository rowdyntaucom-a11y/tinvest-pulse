import fs from"node:fs";import assert from"node:assert/strict";const src=fs.readFileSync(new URL("../src/home/V3Home.tsx",import.meta.url),"utf8");assert.match(src,/const trusted=model\.isTrusted/);assert.match(src,/tm=\(v:number\|null\)=>trusted\?m\(v\):"—"/);assert.match(src,/history=trusted\?model\.history:\[\]/);assert.match(src,/leaders=trusted\?model\.leaders:\[\]/);assert.match(src,/best=trusted\?model\.best:null/);assert.match(src,/worst=trusted\?model\.worst:null/);assert.match(src,/trusted\?`\$\{history\.length\} точек`:"Нет подтверждения"/);console.log("home trust boundary regression: ok");
