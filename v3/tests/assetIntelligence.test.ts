import assert from"node:assert/strict";import{readFileSync}from"node:fs";const w=readFileSync(new URL("../src/assets/V3AssetWorkspace.tsx",import.meta.url),"utf8"),a=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/assetWorkspace.css",import.meta.url),"utf8");for(const label of["Показатели","Доход"])assert.match(w,new RegExp(label));assert.match(w,/loadAssetFundamentals/);assert.match(w,/formatAssetFundamentalMetric/);assert.match(w,/GetAssetFundamentals/);assert.match(w,/QVANIX‑оценка намеренно не строится/);assert.match(w,/loadPayoutCalendar/);assert.match(w,/buildIncomeSourceRows/);assert.match(w,/row\.matchBasis==="FIGI"/);assert.match(w,/Факт и будущее расписание не складываются/);assert.match(w,/YoC показывается только при точной идентичности/);assert.match(a,/positions={positions}/);assert.match(c,/v3-fundamental-groups/);assert.match(c,/v3-asset-income-grid/);assert.match(c,/repeat\(6,minmax\(66px,1fr\)\)/);assert.doesNotMatch(w,/buy|sell|купить|продать|Math\.random/i);console.log("v3 asset intelligence contracts: ok");
