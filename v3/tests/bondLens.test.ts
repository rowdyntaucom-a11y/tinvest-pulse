import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/analysis/V3Analysis.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(s,/item=>item\.bond/);assert.match(s,/maturityDate/);assert.match(s,/floatingCoupon===true/);assert.match(s,/amortizing===true/);assert.match(s,/Облигационный слой/);assert.match(s,/Ближайшее погашение/);assert.match(s,/ratioToPercent\(bonds\.weight\)/);assert.match(c,/v3-bond-lens/);assert.doesNotMatch(s,/yieldToMaturity/);console.log("v3 bond lens contracts: ok");
