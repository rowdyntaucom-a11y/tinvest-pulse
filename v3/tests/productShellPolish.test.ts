import assert from"node:assert/strict";import{readFileSync}from"node:fs";const a=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(a,/v3-topbar/);assert.match(a,/v3-brand/);assert.match(a,/aria-label="QVANIX"/);assert.match(c,/focus-visible/);assert.match(c,/touch-action:manipulation/);assert.match(c,/env\(safe-area-inset-top\)/);assert.match(c,/prefers-reduced-motion:reduce/);assert.match(c,/data-shell="horizon"[^]*v3-analysis-signal/);console.log("v3 product shell polish contracts: ok");
