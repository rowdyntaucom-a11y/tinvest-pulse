import assert from"node:assert/strict";import{buildAssetHistoryGeometry}from"../src/assets/assetHistoryGeometry.ts";const points=[{date:"2026-01-01",value:100},{date:"2026-01-02",value:110},{date:"2026-01-11",value:90}];const g=buildAssetHistoryGeometry(points);assert.ok(g);assert.equal(g.coords[0].x,4);assert.equal(g.coords[2].x,96);const firstGap=g.coords[1].x-g.coords[0].x,lastGap=g.coords[2].x-g.coords[1].x;assert.ok(lastGap>firstGap*8);assert.equal(g.max,110);assert.equal(g.min,90);assert.equal(buildAssetHistoryGeometry([{date:"2026-01-01",value:100}]),null);console.log("v3 asset history time geometry: ok");
