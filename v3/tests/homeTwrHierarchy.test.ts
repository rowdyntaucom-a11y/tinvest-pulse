import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{buildV3HomeViewModel}from"../src/home/homeViewModel.ts";import type{PortfolioSnapshot}from"../../v2/src/lib/portfolioApi.ts";const base={accountName:"Test",value:120,profit:20,profitPct:20,passiveIncome:4,averageMonthlyPassiveIncome:1,averageAnnualPassiveIncome:12,positions:1,positionItems:[],xirr:.21,cagr:.17,riskFreeRate:null,riskFreeRateDate:null,nextRateMeeting:null,startDate:null,updatedAt:"2026-09-19T00:00:00Z",history:[{date:"2026-01-01",portfolio:.1,imoex:.05,value:100,invested:90},{date:"2026-02-01",portfolio:null,imoex:.06,value:110,invested:95},{date:"2026-03-01",portfolio:.14,imoex:.07,value:120,invested:100}],source:"portfolio"} satisfies PortfolioSnapshot;const live=buildV3HomeViewModel(base,true);assert.equal(live.twr,.14);assert.equal(buildV3HomeViewModel({...base,history:[...base.history,{date:"2026-04-01",portfolio:null,imoex:.08,value:121,invested:100}]},true).twr,.14);assert.equal(buildV3HomeViewModel(base,false).twr,null);const h=readFileSync(new URL("../src/home/V3Home.tsx",import.meta.url),"utf8");const primary=h.slice(h.indexOf('<section className="v3-primary-grid">'),h.indexOf('<section className="v3-history-card">'));assert.match(primary,/TWR <V3MetricHelp topic="twr"/);assert.match(primary,/Пассивный доход/);assert.match(primary,/XIRR/);assert.doesNotMatch(primary,/CAGR/);assert.match(h,/CAGR <V3MetricHelp topic="cagr"/);assert.match(h,/Стратегия · без внешних потоков/);assert.match(h,/Личный результат · с потоками/);console.log("v3 Pult TWR hierarchy contracts: ok");
