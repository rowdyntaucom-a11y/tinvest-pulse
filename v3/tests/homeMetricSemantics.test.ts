import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/home/V3Home.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(s,/v3-metric is-cash/);assert.match(s,/v3-metric is-count/);assert.equal((s.match(/v3-metric is-rate/g)||[]).length,2);assert.match(c,/font-variant-numeric:tabular-nums/);assert.match(c,/is-rate span:after/);assert.match(c,/is-cash:after/);console.log("v3 Home metric semantics contracts: ok");
