import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/analysis/V3Analysis.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(s,/x\.portfolio!=null&&x\.imoex!=null/);assert.match(s,/portfolio-imoex/);assert.match(s,/IMOEX/);assert.match(s,/общим точкам доступной истории/);assert.match(s,/не прогноз/);assert.match(s,/mode==="detailed"&&section==="market"&&bm/);assert.match(s,/Период сравнения с IMOEX/);assert.match(c,/v3-benchmark/);assert.doesNotMatch(s,/Math\.random/);console.log("v3 benchmark analysis contracts: ok");
