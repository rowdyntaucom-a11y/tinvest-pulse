import assert from"node:assert/strict";import{readFileSync}from"node:fs";const main=readFileSync(new URL("../src/main.tsx",import.meta.url),"utf8"),guide=readFileSync(new URL("../src/demo/V3DemoGuide.tsx",import.meta.url),"utf8"),css=readFileSync(new URL("../src/styles/demo.css",import.meta.url),"utf8");assert.match(main,/import\{V3DemoGuide\}from"\.\/demo\/V3DemoGuide"/);assert.match(main,/function DemoRoot\(\)[^]*<V3DemoGuide\/>/);assert.doesNotMatch(main,/function LiveRoot\(\)[^]*<V3DemoGuide\/>/);assert.match(guide,/Синтетические данные|синтетические/);assert.match(guide,/брокерский токен/);assert.match(guide,/не выполняется загрузка личного портфеля/);assert.match(guide,/role="dialog"/);assert.match(guide,/aria-modal="true"/);assert.match(guide,/event\.key==="Escape"/);assert.match(guide,/ArrowRight/);assert.match(guide,/Как смотреть демо/);assert.match(css,/\.v3-demo-guide-backdrop/);assert.match(css,/min-height:44px/);assert.doesNotMatch(guide,/fetch\(|loadV3Portfolio|localStorage|sessionStorage/);console.log("v3 demo guided tour contracts: ok");
