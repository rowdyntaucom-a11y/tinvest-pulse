import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/income/V3Income.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(s,/v3-income-context/);assert.match(s,/model\.total\/model\.monthly/);assert.match(s,/не прогноз будущего дохода/);assert.match(s,/не обещание будущих выплат/);assert.match(s,/Пополнения счёта не считаются доходом/);assert.match(c,/v3-income-context/);assert.match(s,/v3-income-depth-cue/);assert.match(s,/scrollIntoView/);assert.match(s,/prefers-reduced-motion/);assert.match(s,/v3-income-depth-anchor/);assert.doesNotMatch(s,/Math\.random|775\.55/);console.log("v3 Income command-center contracts: ok");
