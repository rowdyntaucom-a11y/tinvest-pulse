import assert from"node:assert/strict";import{readFileSync}from"node:fs";const app=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8"),css=readFileSync(new URL("../src/styles/dataState.css",import.meta.url),"utf8"),main=readFileSync(new URL("../src/main.tsx",import.meta.url),"utf8");for(const state of["loading","live","untrusted","error"])assert.match(app,new RegExp(`${state}:\\{title:`));assert.match(app,/aria-expanded=\{stateOpen\}/);assert.match(app,/aria-controls="v3-data-state-detail"/);assert.match(app,/не заменяются нулями или догадками/);assert.match(app,/не становятся подтверждёнными автоматически/);assert.match(css,/prefers-reduced-motion:reduce/);assert.match(css,/data-shell="horizon"/);assert.match(css,/data-shell="carbon"/);assert.match(main,/styles\/dataState\.css/);console.log("v3 data-state recovery contracts: ok");