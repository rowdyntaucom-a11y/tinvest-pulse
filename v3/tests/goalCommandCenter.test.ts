import assert from"node:assert/strict";import{readFileSync}from"node:fs";const g=readFileSync(new URL("../src/goal/V3Goal.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/goal.css",import.meta.url),"utf8");assert.doesNotMatch(g,/window\.prompt|window\.location\.reload/);assert.match(g,/inputMode="decimal"/);assert.match(g,/localStorage\.setItem\(TARGET_KEY/);assert.match(g,/trusted&&value!=null/);assert.match(g,/без прогноза срока/);assert.match(g,/нет предполагаемой доходности/);assert.match(c,/min-height:46px/);assert.match(c,/grid-template-columns:1fr 1fr/);console.log("v3 goal command center contracts: ok");
