import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/analysis/V3Analysis.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(s,/useState<"overview"\|"structure"\|"market">/);for(const x of ["Обзор","Структура","Рынок"])assert.match(s,new RegExp(">"+x+"<"));assert.match(s,/section==="overview"&&rows\.length/);assert.match(s,/section==="structure"&&bonds/);assert.match(s,/section==="market"&&bm/);assert.match(s,/aria-label="Слои аналитики"/);assert.match(c,/v3-analysis-tabs/);assert.match(c,/position:sticky/);console.log("v3 Analysis layer contracts: ok");
