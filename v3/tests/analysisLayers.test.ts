import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/analysis/V3Analysis.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/analysisDepth.css",import.meta.url),"utf8");assert.match(s,/useState<"overview"\|"return"\|"risk"\|"structure"\|"market">/);for(const x of["Обзор","Доходность","Риск","Структура","Рынок"])assert.match(s,new RegExp('"'+x+'"'));for(const layer of["V3ReturnLayer","V3RiskLayer","V3MarketLayer"])assert.match(s,new RegExp(layer));assert.match(s,/aria-label="Слои аналитики"/);assert.match(c,/v3-analysis-tabs\.is-depth/);assert.match(c,/repeat\(5/);console.log("v3 Analysis layer contracts: ok");
