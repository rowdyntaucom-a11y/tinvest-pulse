import assert from"node:assert/strict";import{readFileSync}from"node:fs";
const s=readFileSync(new URL("../src/analysis/V3Analysis.tsx",import.meta.url),"utf8"),selector=readFileSync(new URL("../src/navigation/V3SectionSelector.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/analysisDepth.css",import.meta.url),"utf8");
assert.match(s,/export type V3AnalysisSection="overview"\|"return"\|"risk"\|"structure"\|"market"/);assert.match(s,/useState<V3AnalysisSection>\(initialSection\)/);assert.match(s,/useEffect\(\(\)=>setSection\(initialSection\),\[initialSection\]\)/);for(const x of["Обзор","Доходность","Риск","Структура","Рынок"])assert.match(s,new RegExp('"'+x+'"'));for(const layer of["V3ReturnLayer","V3RiskLayer","V3MarketLayer"])assert.match(s,new RegExp(layer));assert.match(s,/V3SectionSelector label="Раздел аналитики"/);assert.match(s,/sectionOptions/);assert.match(selector,/aria-label={label}/);assert.doesNotMatch(s,/v3-analysis-tabs/);assert.doesNotMatch(c,/v3-analysis-tabs/);console.log("v3 Analysis layer contracts: ok");