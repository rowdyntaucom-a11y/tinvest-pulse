import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");for(const shell of ["core","horizon","carbon"])assert.match(s,new RegExp(`data-shell="${shell}"`));assert.match(s,/horizon[^]*#eef2f4/);assert.match(s,/carbon[^]*#050610/);assert.match(s,/core[^]*#080b0e/);assert.match(s,/HORIZON|horizon/);console.log("v3 shell identity contracts: ok");
