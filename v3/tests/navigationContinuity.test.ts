import assert from"node:assert/strict";import test from"node:test";import{readFileSync}from"node:fs";import{navigationEntry,readNavigationState,workspaceFromLocation}from"../src/navigation/navigationHistory.ts";
const hook=readFileSync(new URL("../src/navigation/useNavigationContinuity.ts",import.meta.url),"utf8");
test("workspace deep link accepts only canonical v3 destinations",()=>{assert.equal(workspaceFromLocation("?view=analysis","home"),"analysis");assert.equal(workspaceFromLocation("?view=unknown","income"),"income")});
test("history state restores canonical workspace layer",()=>{assert.deepEqual(readNavigationState(navigationEntry({workspace:"assets",layer:"workspace"}),"home"),{workspace:"assets",layer:"workspace"})});
test("asset layer requires a ticker and fails closed to its workspace",()=>{assert.deepEqual(readNavigationState({qvanixV3Navigation:{workspace:"assets",layer:"asset"}},"home"),{workspace:"assets",layer:"workspace"});assert.deepEqual(readNavigationState(navigationEntry({workspace:"analysis",layer:"asset",assetTicker:"LKOH"}),"home"),{workspace:"analysis",layer:"asset",assetTicker:"LKOH"})});
test("pulse remains an overlay over the originating workspace",()=>{assert.deepEqual(readNavigationState(navigationEntry({workspace:"home",layer:"pulse"}),"assets"),{workspace:"home",layer:"pulse"})});
test("history accepts only finite non-negative scroll positions",()=>{assert.deepEqual(readNavigationState(navigationEntry({workspace:"assets",layer:"workspace",scrollY:481.6}),"home"),{workspace:"assets",layer:"workspace",scrollY:482});assert.deepEqual(readNavigationState({qvanixV3Navigation:{workspace:"assets",layer:"workspace",scrollY:-1}},"home"),{workspace:"assets",layer:"workspace"});assert.deepEqual(readNavigationState({qvanixV3Navigation:{workspace:"assets",layer:"workspace",scrollY:"400"}},"home"),{workspace:"assets",layer:"workspace"})});
test("navigation owns browser scroll restoration and snapshots before push",()=>{assert.match(hook,/scrollRestoration="manual"/);assert.match(hook,/replaceState\(navigationEntry\(atScroll\(current\)\)/);assert.match(hook,/scrollY:0/);assert.match(hook,/restoreScroll\(resolved\)/);assert.match(hook,/requestAnimationFrame/)});
