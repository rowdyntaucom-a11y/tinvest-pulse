import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/data/loadV3Portfolio.ts",import.meta.url),"utf8"),m=readFileSync(new URL("../src/main.tsx",import.meta.url),"utf8"),g=readFileSync(new URL("../src/data/loadState.ts",import.meta.url),"utf8");assert.match(s,/loadPortfolio/);assert.match(s,/evaluateDataTrust/);assert.match(s,/staleAfterMs:15\*60_000/);assert.doesNotMatch(s,/fetch\(/);assert.match(m,/loadV3Portfolio/);assert.match(m,/isV3VerifiedLive\(trust\)/);assert.match(m,/toV3LoadState\(trust\)/);assert.match(g,/trust\.verifiedLive&&trust\.safeToCalculate/);assert.doesNotMatch(m,/trust\.safeToDisplay\?"live"/);assert.doesNotMatch(m,/84848|12436|17\.2/);console.log("v3 verified live data bridge contracts: ok");
