import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/data/loadV3Portfolio.ts",import.meta.url),"utf8"),m=readFileSync(new URL("../src/main.tsx",import.meta.url),"utf8");assert.match(s,/loadPortfolio/);assert.match(s,/evaluateDataTrust/);assert.match(s,/evaluateDataTrust/);assert.match(s,/staleAfterMs:15\*60_000/);assert.doesNotMatch(s,/fetch\(/);assert.match(m,/loadV3Portfolio/);assert.match(m,/trust\.safeToDisplay/);assert.doesNotMatch(m,/84848|12436|17\.2/);console.log("v3 live data bridge contracts: ok");
