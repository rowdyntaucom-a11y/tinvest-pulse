import assert from"node:assert/strict";import{readFileSync}from"node:fs";const a=readFileSync(new URL("../src/app/V3App.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(a,/v3-atmosphere/);assert.match(a,/aria-hidden="true"/);assert.match(c,/pointer-events:none/);assert.match(c,/data-shell="core"[^]*clip-path:polygon/);assert.match(c,/data-shell="horizon"[^]*#f9fbfc/);assert.match(c,/data-shell="carbon"[^]*background-size:36px 36px/);assert.match(c,/prefers-reduced-motion:reduce/);assert.doesNotMatch(a,/img src|https?:\/\//);console.log("v3 shell atmosphere contracts: ok");
