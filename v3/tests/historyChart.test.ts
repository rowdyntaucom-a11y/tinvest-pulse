import assert from"node:assert/strict";import{readFileSync}from"node:fs";const s=readFileSync(new URL("../src/home/V3HistorySparkline.tsx",import.meta.url),"utf8"),c=readFileSync(new URL("../src/styles/v3.css",import.meta.url),"utf8");assert.match(s,/v3-history-figure/);assert.match(s,/polygon/);assert.match(s,/polyline/);assert.match(s,/figcaption/);assert.match(s,/last\/first-1/);assert.match(s,/rows\[0\]\.date/);assert.match(s,/aria-label="История стоимости портфеля"/);assert.match(c,/v3-chart-area/);assert.doesNotMatch(s,/Math\.random/);console.log("v3 history chart contracts: ok");
